package com.discut.manga.ui.security

import androidx.appcompat.app.AppCompatActivity

interface SecurityActivityDelegate {

    fun registerSecurityActivity(activity: AppCompatActivity)

    companion object{

    }

}