package com.discut.manga.ui.reader.viewer.horizontal

import android.content.Context
import com.discut.manga.ui.reader.page.PageTransitionView

class HorizontalTransitionHolder(
    context: Context
) : PageTransitionView(context)