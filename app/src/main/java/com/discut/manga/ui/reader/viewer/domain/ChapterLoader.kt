package com.discut.manga.ui.reader.viewer.domain

import com.discut.manga.util.withIOContext

class ChapterLoader {

    suspend fun loadChapter(chapterId: Long) {
        withIOContext {

        }
    }
}